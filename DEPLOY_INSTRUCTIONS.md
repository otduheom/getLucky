# Инструкция по деплою на Render.com

## ✅ Что уже настроено

Все необходимые настройки для деплоя уже выполнены:

1. ✅ `vite.config.ts` - сборка настроена на `../server/dist`
2. ✅ `server/.gitignore` - папка `dist` добавлена в игнор
3. ✅ `server/src/app.js` - настроена раздача статических файлов и catch-all маршрут
4. ✅ `server/package.json` - скрипты для production уже настроены
5. ✅ `server/db/database.js` - конфигурация для production готова
6. ✅ `client/src/shared/lib/axiosInstance.ts` - baseURL установлен на `/api` (работает в production)
7. ✅ `client/src/shared/lib/socketInstance.ts` - Socket.IO использует `window.location.origin` в production

## 📋 Шаги для деплоя

### 1. Подготовка репозитория

Если еще не создан репозиторий на GitHub:

```bash
# В корне проекта
git init
git add .
git commit -m "Initial commit"
# Создайте репозиторий на GitHub и выполните:
git remote add origin <ваш-repo-url>
git push -u origin main
```

### 2. Создание базы данных PostgreSQL на Render.com

1. Войдите на [render.com](https://render.com/)
2. Нажмите **New** → **PostgreSQL**
3. Заполните форму:
   - **Name**: выберите имя для базы данных
   - **Database**: имя базы данных (или оставьте по умолчанию)
   - **User**: имя пользователя (или оставьте по умолчанию)
   - **Region**: выберите ближайший регион
   - **PostgreSQL Version**: оставьте последнюю версию
   - **Plan**: выберите план (Free tier доступен)
4. Нажмите **Create Database**
5. После создания БД:
   - Перейдите в настройки базы данных
   - Скопируйте **Internal Database URL** (он понадобится для переменных окружения)

### 3. Создание Web Service на Render.com

1. Нажмите **New** → **Web Service**
2. Подключите ваш GitHub репозиторий
3. Заполните настройки:

   **Основные настройки:**
   - **Name**: выберите имя для сервиса
   - **Region**: выберите тот же регион, что и для БД
   - **Branch**: `main` (или ваша ветка для деплоя)
   - **Root Directory**: оставьте пустым (корень проекта)

   **Build & Deploy:**
   - **Build Command**: 
     ```bash
     cd client && npm install && npm run build && cd ../server && npm install
     ```
   - **Start Command**: 
     ```bash
     cd server && npm start
     ```
   - **Environment**: `Node`

4. Перейдите в раздел **Environment Variables** и добавьте:

   **Обязательные переменные:**
   ```
   NODE_ENV=production
   DB_URL_PROD=<Internal Database URL из шага 2>
   ACCESS_TOKEN_SECRET=<ваш-секретный-ключ-для-access-token>
   REFRESH_TOKEN_SECRET=<ваш-секретный-ключ-для-refresh-token>
   ```

   **Опциональные переменные (если используются):**
   ```
   GIGACHAT_KEY=<ваш-gigachat-ключ>
   GIGACHAT_SCOPE=GIGACHAT_API_PERS
   PORT=10000
   ```

   ⚠️ **ВАЖНО**: Используйте сильные случайные строки для `ACCESS_TOKEN_SECRET` и `REFRESH_TOKEN_SECRET`!

5. Нажмите **Create Web Service**

### 4. Настройка локального .env для production БД (опционально)

Если хотите запускать миграции локально для production БД, добавьте в `server/.env`:

```env
# Production база данных (для локальных миграций)
DB_URL_PROD=<External Database URL из Render.com>
# или отдельные параметры:
# DB_USER_PROD=...
# DB_NAME_PROD=...
# DB_PASS_PROD=...
# DB_HOST_PROD=...
```

⚠️ **ВНИМАНИЕ**: Используйте **External Database URL** для локальных подключений, а **Internal Database URL** - только в переменных окружения на Render.com.

### 5. Запуск миграций базы данных

После создания Web Service на Render.com, вам нужно выполнить миграции. Есть два способа:

#### Способ 1: Через Render.com Shell (рекомендуется)

1. В настройках Web Service найдите **Shell**
2. Откройте shell и выполните:
   ```bash
   cd server
   npm run db:setup
   ```

#### Способ 2: Локально (если настроили DB_URL_PROD)

1. Добавьте переменные окружения для production в `server/.env`
2. Выполните:
   ```bash
   cd server
   npm run db:setup
   ```

⚠️ **Примечание**: `db:setup` выполняет миграции и сидеры. Если сидеры не нужны в production, можно выполнить только:
```bash
NODE_ENV=production npx sequelize db:migrate
```

### 6. Проверка деплоя

1. После деплоя перейдите на URL вашего сервиса (будет показан в Dashboard)
2. Проверьте логи в разделе **Logs** на наличие ошибок
3. Попробуйте зарегистрироваться/войти в систему
4. Проверьте работу основных функций

## 🔧 Дополнительная настройка

### Настройка домена (опционально)

1. В настройках Web Service перейдите в **Custom Domains**
2. Добавьте ваш домен
3. Настройте DNS записи согласно инструкциям Render.com

### Переменные окружения для разных сред

Рекомендуется использовать разные ключи для development и production:

- **Development** (локально): `ACCESS_TOKEN_SECRET`, `REFRESH_TOKEN_SECRET`
- **Production** (Render.com): те же переменные, но с другими значениями

## 📝 Примечания

1. **База данных**: На бесплатном плане Render.com база данных "засыпает" после периода неактивности. Первый запрос после этого может занять больше времени.

2. **WebSocket**: Socket.IO должен работать автоматически, так как настроен на использование `window.location.origin` в production.

3. **Статические файлы**: Сборка клиента будет автоматически раздаваться через Express из папки `server/dist`.

4. **Логи**: Все логи доступны в реальном времени в разделе **Logs** вашего Web Service на Render.com.

5. **Переменные окружения**: После изменения переменных окружения на Render.com нужно перезапустить сервис (происходит автоматически при сохранении).

## 🐛 Решение проблем

### Ошибка подключения к БД

- Убедитесь, что используете **Internal Database URL** в переменных окружения
- Проверьте, что база данных создана и запущена

### Ошибка при сборке

- Проверьте логи сборки в Render.com
- Убедитесь, что все зависимости указаны в `package.json`
- Проверьте, что Node.js версия совместима (Render.com автоматически определяет версию из `package.json`)

### 404 при переходе на страницы приложения

- Убедитесь, что catch-all маршрут (`app.get('/*', ...)`) находится в конце `app.js`
- Проверьте, что статические файлы раздаются правильно

### Socket.IO не работает

- Проверьте, что CORS настроен правильно
- Убедитесь, что Socket.IO использует `window.location.origin` в production (уже настроено)

## ✅ Чек-лист перед деплоем

- [ ] Код закоммичен и запушен в GitHub
- [ ] База данных PostgreSQL создана на Render.com
- [ ] Web Service создан и настроен
- [ ] Все переменные окружения добавлены
- [ ] Build Command указан правильно
- [ ] Start Command указан правильно
- [ ] Миграции выполнены
- [ ] Приложение работает после деплоя

Удачи с деплоем! 🚀
